#include "SkiaPictureViewComponent.h"

namespace facebook::react {

// The name the package's JavaScript registers, from
// `codegenNativeComponent<NativeProps>("SkiaPictureView")`. No adapter prefix:
// this is a codegen component rather than an Expo view manager, so the name is
// the name.
extern const char SkiaPictureViewComponentName[] = "SkiaPictureView";

} // namespace facebook::react
